import{useCallback,useEffect,useState,type FormEvent}from"react";import{Plus}from"lucide-react";import toast from"react-hot-toast";import{EmptyState,ErrorState,SkeletonState}from"@/components/AsyncState";import Modal from"@/components/Modal";import PageHeader from"@/components/PageHeader";import DashboardLayout from"@/layouts/DashboardLayout";import{Api}from"@/services/http";type Campaign={id:string;name:string;channel:string;segment:string;status:string;message:string;audience_count:number};type Page={results:Campaign[]};const field="h-11 w-full rounded-xl border border-[var(--cfit-border)] bg-[var(--cfit-surface-elevated)] px-3";export default function Relationship(){const[data,setData]=useState<Campaign[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(false),[open,setOpen]=useState(false),[saving,setSaving]=useState(false),[form,setForm]=useState({name:"",channel:"whatsapp",segment:"at_risk",message:""});const load=useCallback(async()=>{try{setLoading(true);setError(false);const response=await Api.get<Page>("/operations/campaigns/");setData(response.data.results)}catch{setError(true)}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);async function save(e:FormEvent){e.preventDefault();setSaving(true);try{await Api.post("/operations/campaigns/",form);setOpen(false);setForm({name:"",channel:"whatsapp",segment:"at_risk",message:""});toast.success("Rascunho salvo. Nenhum envio foi realizado.");await load()}finally{setSaving(false)}}async function prepare(id:string){await Api.post(`/operations/campaigns/${id}/prepare/`,{provider:"sandbox"});toast.success("Público preparado em sandbox; nenhum envio externo realizado.");await load()}return <DashboardLayout><div className="space-y-5"><PageHeader title="Relacionamento"subtitle="Campanhas, segmentos e histórico com sandbox explícito."eyebrow="Comunicação segura"context="Rascunho antes de qualquer execução"actions={<button onClick={()=>setOpen(true)}className="cfit-primary-button"><Plus size={16}/>Nova campanha</button>}/>{loading?<SkeletonState rows={5}/>:error?<ErrorState onRetry={load}/>:data.length===0?<EmptyState label="Nenhuma campanha"detail="Crie um rascunho sem realizar envios externos."/>:<section className="cfit-record-list rounded-2xl border border-[var(--cfit-border)] bg-[var(--cfit-surface-primary)]">{data.map(item=><article key={item.id}className="p-5"><div className="flex flex-wrap justify-between gap-3"><div><strong>{item.name}</strong><p className="mt-1 text-sm text-[var(--cfit-text-secondary)]">{item.channel} · {item.segment} · {item.status}</p></div><button onClick={()=>void prepare(item.id)}className="cfit-secondary-button">Preparar em sandbox</button></div><p className="mt-3 text-sm">{item.message}</p></article>)}</section>}<Modal open={open}title="Nova campanha"onClose={()=>!saving&&setOpen(false)}><form onSubmit={save}className="space-y-3"><input required placeholder="Nome"value={form.name}onChange={e=>setForm({...form,name:e.target.value})}className={field}/><div className="grid gap-3 sm:grid-cols-2"><select value={form.channel}onChange={e=>setForm({...form,channel:e.target.value})}className={field}><option value="whatsapp">WhatsApp</option><option value="email">E-mail</option></select><select value={form.segment}onChange={e=>setForm({...form,segment:e.target.value})}className={field}><option value="at_risk">Em risco</option><option value="defaulting">Inadimplentes</option><option value="inactive">Inativos</option><option value="all_active">Todos ativos</option></select></div><textarea required placeholder="Conteúdo"value={form.message}onChange={e=>setForm({...form,message:e.target.value})}className="min-h-32 w-full rounded-xl border border-[var(--cfit-border)] bg-[var(--cfit-surface-elevated)] p-3"/><p className="text-xs text-[var(--cfit-text-secondary)]">Salvar não envia mensagens. A preparação usa sandbox por padrão.</p><button disabled={saving}className="cfit-primary-button">{saving?"Salvando...":"Salvar rascunho"}</button></form></Modal></div></DashboardLayout>}
